int main() {
    int a, b, operacion, resultado;

* // --- ENTRADA DE DATOS ---
    printf("Ingresa el primer valor entero (a): ");
    scanf("%d", &a);

    printf("Ingresa el segundo valor entero (b): ");
    scanf("%d", &b); * // Un espacio para que se vea más ordenado

 * // --- SELECCIONAR OPERACIÓN ---
    printf("\nEscoge una operacion a nivel de bits:\n");
    printf("1. AND (&)\n");
    printf("2. OR (|)\n");
    printf("3. XOR (^)\n");
    printf("Tu eleccion: ");
    scanf("%d", &operacion);

    printf("\n");
       switch (operacion) {
        case 1:
            printf("Operacion: AND (&)\n");
            printf("Tabla de verdad (para cada bit):\n");
            printf(" - 0 & 0 -> 0\n");
            printf(" - 0 & 1 -> 0\n");
            printf(" - 1 & 0 -> 0\n");
            printf(" - 1 & 1 -> 1\n");
            resultado = a & b;
            break;

        case 2:
            printf("Operacion: OR (|)\n");
            printf("Tabla de verdad (para cada bit):\n");
            printf(" - 0 | 0 -> 0\n");
            printf(" - 0 | 1 -> 1\n");
            printf(" - 1 | 0 -> 1\n");
            printf(" - 1 | 1 -> 1\n");
            resultado = a | b;
            break;
         case 3:
            printf("Operacion: XOR (^)\n");
            printf("Tabla de verdad (para cada bit):\n");
            printf("  - 0 ^ 0 -> 0\n");
            printf("  - 0 ^ 1 -> 1\n");
            printf("  - 1 ^ 0 -> 1\n");
            printf("  - 1 ^ 1 -> 0\n");
            resultado = a ^ b;
            break;

        default:
            printf("Error: La opcion '%d' no es valida.\n", operacion);
            return 1;
    }
 printf("\n----------------------------------\n");
    printf("El resultado es: %d\n", resultado);
    printf("----------------------------------\n");

    return 0;
}
